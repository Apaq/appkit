import { Component, h } from '@stencil/core';

@Component({
  tag: 'ak-documents',
  styleUrl: 'documents.css',
  shadow: true,
})
export class Documents {
  render() {
    return (
        <span>Documents</span>
    );
  }
}