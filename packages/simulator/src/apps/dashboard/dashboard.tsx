import { Component, h } from '@stencil/core';

@Component({
  tag: 'ak-dashboard',
  styleUrl: 'dashboard.css',
  shadow: true,
})
export class Dashboard {
  render() {
    return (
        Dashboard
    );
  }
}